var React = require('react');

var ContactContainer = React.createClass({

  render: function() {
    return (
      <div className="container">
        <p>Contact list + info</p>
      </div>
    );
  },

});

module.exports = ContactContainer;