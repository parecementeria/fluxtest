var React = require('react');

var Header = React.createClass({

  render: function() {
    return (
      <div className="contactBox container">
        <h2 className="title">Flux-React Contact List</h2>
      </div>
    );
  },

});

module.exports = Header;